name: SBCrafter
version: '${project.version}'
main: me.autobot.sbcrafter.SBCrafter
api-version: '1.17'
folia-supported: true
load: POSTWORLD
commands:
  SBCrafter:
    aliases:
      - sbc
    usage: /command recipe on|off